<Page
    x:Class="BubbleShot.UniversalApp.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="using:Prism.Windows.Mvvm"
    xmlns:viewModels="using:BubbleShot.UniversalApp.ViewModels"
    mc:Ignorable="d" mvvm:ViewModelLocator.AutoWireViewModel="true" d:DataContext="{d:DesignInstance viewModels:MainPageViewModel }">
    <Page.TopAppBar>
        <AppBar>
            <StackPanel Orientation="Horizontal">
                <AppBarButton Icon="Play" Label="Start" Command="{Binding StartAdapterCommand}"/>
                <AppBarButton Icon="Stop" Label="Stop" Command="{Binding StopAdapterCommand}"/>
            </StackPanel>
        </AppBar>
    </Page.TopAppBar>
    <FlipView>
        <StackPanel>
            <TextBlock Text="Карта" />
        </StackPanel>
        <StackPanel>
            <TextBlock Text="Галерея" />
        </StackPanel>
    </FlipView>
</Page>
