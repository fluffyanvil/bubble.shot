<Page x:ConnectionId='1'
    x:Class="BubbleShot.UniversalApp.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="using:Prism.Windows.Mvvm"
    xmlns:viewModels="using:BubbleShot.UniversalApp.ViewModels"
    xmlns:maps="using:Windows.UI.Xaml.Controls.Maps"
    xmlns:models="using:BubbleShot.UniversalApp.Models"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:interactivity1="using:Microsoft.Xaml.Interactivity"
    xmlns:maps1="using:Windows.Services.Maps"
    mc:Ignorable="d" mvvm:ViewModelLocator.AutoWireViewModel="true" d:DataContext="{d:DesignInstance viewModels:MainPageViewModel }"                                     >
    <Page.Resources>
    </Page.Resources>
    <Grid>
        <FlipView HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <FlipViewItem IsSelected="{Binding ElementName=MapToggleButton, Path=IsChecked, Mode=TwoWay}">
                <Grid Background="AntiqueWhite">
                    <maps:MapControl x:ConnectionId='7' x:Name="Map" Style="Road"
                             MapServiceToken="uhWN34e4LCVOVBNwA3ZI~bzzP9OY-SZ_ay7FVUskS-w~Art6O_89BNl-_04hlwpqkWl7zErWSJdwuw3kz47zxb9FZ-nyJQKESG87Mh4wvI_G"
                             ZoomInteractionMode="GestureAndControl"
                             LandmarksVisible="True"
                             IsRightTapEnabled="True"
                                                                                                        >
                        <maps:MapItemsControl>
                            <maps:MapItemsControl.Items>
                                <Border Width="20" Height="20" maps:MapControl.Location="{Binding DeviceLocation}">
                                    <Ellipse x:ConnectionId='8' Fill="Red" Stroke="DarkRed" x:Name="Ellipse"                           Visibility="{Binding Location, Converter={StaticResource NullToVisibilityConverter}}">
                                        <Ellipse.Resources>
                                            <Storyboard x:ConnectionId='9' x:Name="BlinkStoryboard">
                                                <DoubleAnimation
                                                    Storyboard.TargetName="Ellipse"
                                                    Storyboard.TargetProperty="Opacity"
                                                    From="1.0" To="0.0" Duration="0:0:0.5"
                                                    AutoReverse="True" RepeatBehavior="Forever"/>
                                            </Storyboard>
                                        </Ellipse.Resources>
                                    </Ellipse>
                                </Border>
                            </maps:MapItemsControl.Items>
                        </maps:MapItemsControl>
                    </maps:MapControl>
                </Grid>
            </FlipViewItem>
            <FlipViewItem IsSelected="{Binding ElementName=GalleryToggleButton, Path=IsChecked, Mode=TwoWay}">
                <Grid>
                    <GridView Margin="15" AutomationProperties.AutomationId="ItemGridView" ItemsSource="{Binding Photos}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
                        <GridView.ItemTemplate>
                            <DataTemplate                                        >
                                <Grid HorizontalAlignment="Left" Width="150" Height="150">
                                    <Border>
                                        <Image Source="{Binding Image}" Stretch="UniformToFill"/>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapGrid ItemWidth="200" 
                                      ItemHeight="200" 
                                      Orientation="Horizontal"
                                      VerticalChildrenAlignment="Top"
                                      HorizontalAlignment="Stretch"/>
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                    </GridView>

                    <Grid HorizontalAlignment="Stretch" Height="{Binding AvailableModalSize}" Width="{Binding AvailableModalSize}" Visibility="{Binding SelectedItem, Converter={StaticResource NullToVisibilityConverter}}">
                        <Border>
                            <Grid>
                                <Image Stretch="UniformToFill" Source="{Binding SelectedItem.Image}">
                                    <interactivity1:Interaction.Behaviors>
                                        <interactivity1:BehaviorCollection>
                                            <core:EventTriggerBehavior EventName="Tapped">
                                                <core:InvokeCommandAction Command="{Binding CLoseDetails}"></core:InvokeCommandAction>
                                            </core:EventTriggerBehavior>
                                        </interactivity1:BehaviorCollection>
                                    </interactivity1:Interaction.Behaviors>
                                </Image>
                                <Border Background="DarkSlateGray" Opacity="0.6" Height="45" VerticalAlignment="Bottom">
                                    <StackPanel>
                                        <HyperlinkButton Opacity="1" Foreground="FloralWhite" NavigateUri="{Binding SelectedItem.UserLink}" Content="{Binding SelectedItem.UserLink}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>

                    </Grid>
                </Grid>
            </FlipViewItem>
        </FlipView>
        <Grid x:ConnectionId='2' x:Name="TopToolBar" VerticalAlignment="Top" Background="DarkGray" Opacity="0.7">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <AppBarButton Command="{Binding StartAdapterCommand}" Label="Старт">
                    <AppBarButton.Icon>
                        <FontIcon FontFamily="{StaticResource FontAwesome}" Glyph="&#xf01d;"></FontIcon>
                    </AppBarButton.Icon>
                </AppBarButton>
                <AppBarButton Command="{Binding StopAdapterCommand}" Label="Стоп">
                    <AppBarButton.Icon>
                        <FontIcon FontFamily="{StaticResource FontAwesome}" Glyph="&#xf28e;"></FontIcon>
                    </AppBarButton.Icon>
                </AppBarButton>
            </StackPanel>
            
            <AutoSuggestBox x:ConnectionId='6' Margin="10,0" Grid.Column="0" VerticalAlignment="Center" QueryIcon="Find" Text="{Binding SearchAddress, Mode=TwoWay}" PlaceholderText="Адрес" 
                                                                                  
                                                                                        
                                        AutoMaximizeSuggestionArea="True"
                                                                                            >
                <AutoSuggestBox.ItemTemplate>
                    <DataTemplate                               >
                        <TextBlock Foreground="DarkSlateGray" Text="{Binding Address.FormattedAddress}"></TextBlock>
                    </DataTemplate>
                </AutoSuggestBox.ItemTemplate>
            </AutoSuggestBox>
            <Slider Margin="10,0" Grid.Column="1" Value="{Binding Radius, Mode=TwoWay}" Minimum="1000" Maximum="100000">
                <Slider.HeaderTemplate>
                    <DataTemplate>
                        <TextBlock>
                            <Run>Радиус</Run>
                            <Run Text="{Binding Radius}"></Run>
                            <Run>м</Run>
                        </TextBlock>
                    </DataTemplate>
                </Slider.HeaderTemplate>
            </Slider>
        </Grid>
        <Grid x:ConnectionId='3' x:Name="BottomToolBar" VerticalAlignment="Bottom" Background="DarkGray" Opacity="0.7">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <AppBarToggleButton x:ConnectionId='4' x:Name="MapToggleButton" Label="Карта" IsChecked="True">
                    <AppBarToggleButton.Icon>
                        <FontIcon FontFamily="{StaticResource FontAwesome}" Glyph="&#xf041;"></FontIcon>
                    </AppBarToggleButton.Icon>
                </AppBarToggleButton>
                <AppBarToggleButton x:ConnectionId='5' x:Name="GalleryToggleButton" Label="Галерея">
                    <AppBarToggleButton.Icon>
                        <FontIcon FontFamily="{StaticResource FontAwesome}" Glyph="&#xf03e;"></FontIcon>
                    </AppBarToggleButton.Icon>
                </AppBarToggleButton>
            </StackPanel>
        </Grid>
    </Grid>

</Page>

