<Page x:ConnectionId='1'
    x:Class="BubbleShot.UniversalApp.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mvvm="using:Prism.Windows.Mvvm"
    xmlns:viewModels="using:BubbleShot.UniversalApp.ViewModels"
    xmlns:maps="using:Windows.UI.Xaml.Controls.Maps"
    xmlns:models="using:BubbleShot.UniversalApp.Models"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:interactivity1="using:Microsoft.Xaml.Interactivity"
    xmlns:maps1="using:Windows.Services.Maps"
    mc:Ignorable="d" mvvm:ViewModelLocator.AutoWireViewModel="true" d:DataContext="{d:DesignInstance viewModels:MainPageViewModel }"                                     >
    <Page.Resources>
    </Page.Resources>
    <Grid>
        <FlipView HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <FlipViewItem IsSelected="{Binding ElementName=MapToggleButton, Path=IsChecked, Mode=TwoWay}">
                <Grid Background="AntiqueWhite">
                    <maps:MapControl x:ConnectionId='8' x:Name="Map" Style="Road"
                             MapServiceToken="uhWN34e4LCVOVBNwA3ZI~bzzP9OY-SZ_ay7FVUskS-w~Art6O_89BNl-_04hlwpqkWl7zErWSJdwuw3kz47zxb9FZ-nyJQKESG87Mh4wvI_G"
                             ZoomInteractionMode="GestureAndControl"
                             LandmarksVisible="True"
                             IsRightTapEnabled="True"
                                                                                                        >
                        <maps:MapItemsControl>
                            <maps:MapItemsControl.Items>
                                <Border Width="65" Height="65" maps:MapControl.Location="{Binding SelectedItemGeopoint}" Visibility="{Binding SelectedItem, Converter={StaticResource NullToVisibilityConverter}}">
                                    <Ellipse Stroke="AliceBlue" StrokeThickness="1" Visibility="{Binding SelectedItem, Converter={StaticResource NullToVisibilityConverter}}">
                                        <Ellipse.Fill>
                                            <ImageBrush Stretch="UniformToFill" ImageSource="{Binding SelectedItem.Image}" />
                                        </Ellipse.Fill>
                                    </Ellipse>
                                </Border>
                                <Border maps:MapControl.Location="{Binding DeviceLocation}" Visibility="{Binding DeviceLocation, Converter={StaticResource NullToVisibilityConverter}}">
                                    <Path Width="24" Height="30" Stretch="Fill" Fill="Red" Stroke="DarkRed" Data="M409.81,160.113C409.79,71.684,338.136,0,249.725,0C161.276,0,89.583,71.684,89.583,160.113
				c0,76.325,119.274,280.238,151.955,334.638c1.72,2.882,4.826,4.641,8.178,4.641c3.351,0,6.468-1.759,8.168-4.631
				C290.545,440.361,409.81,236.438,409.81,160.113z M249.716,283.999c-68.303,0-123.915-55.573-123.915-123.895
				c0-68.313,55.592-123.895,123.915-123.895s123.876,55.582,123.876,123.895S318.029,283.999,249.716,283.999z"/>
                                </Border>
                            </maps:MapItemsControl.Items>
                        </maps:MapItemsControl>
                    </maps:MapControl>
                    <Grid x:ConnectionId='9' x:Name="TopToolBar" VerticalAlignment="Top" Background="DarkGray" Opacity="0.7">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                            <ColumnDefinition Width="Auto"></ColumnDefinition>
                        </Grid.ColumnDefinitions>
                        <AutoSuggestBox x:ConnectionId='10' Margin="20,0" Grid.Column="0" VerticalAlignment="Center" QueryIcon="Find" Text="{Binding SearchAddress, Mode=TwoWay}" PlaceholderText="Адрес" 
                                                                                  
                                                                                        
                                        AutoMaximizeSuggestionArea="True"
                                                                                            >
                            <AutoSuggestBox.ItemTemplate>
                                <DataTemplate                               >
                                    <TextBlock Foreground="DarkSlateGray" Text="{Binding Address.FormattedAddress}"></TextBlock>
                                </DataTemplate>
                            </AutoSuggestBox.ItemTemplate>
                        </AutoSuggestBox>
                        <Slider Margin="10,0" Grid.Column="1" Value="{Binding Radius, Mode=TwoWay}" Minimum="1000" Maximum="50000">
                            <Slider.HeaderTemplate>
                                <DataTemplate>
                                    <TextBlock>
                            <Run>Радиус</Run>
                            <Run Text="{Binding Radius}"></Run>
                            <Run>м</Run>
                                    </TextBlock>
                                </DataTemplate>
                            </Slider.HeaderTemplate>
                        </Slider>
                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                            <AppBarButton Command="{Binding StartAdapterCommand}" Label="Старт">
                                <AppBarButton.Icon>
                                    <FontIcon FontFamily="{StaticResource FontAwesome}" Glyph="&#xf01d;"></FontIcon>
                                </AppBarButton.Icon>
                            </AppBarButton>
                            <AppBarButton Command="{Binding StopAdapterCommand}" Label="Стоп">
                                <AppBarButton.Icon>
                                    <FontIcon FontFamily="{StaticResource FontAwesome}" Glyph="&#xf28e;"></FontIcon>
                                </AppBarButton.Icon>
                            </AppBarButton>
                        </StackPanel>
                    </Grid>
                </Grid>
            </FlipViewItem>
            <FlipViewItem IsSelected="{Binding ElementName=GalleryToggleButton, Path=IsChecked, Mode=TwoWay}">
                <Grid>
                    <GridView x:ConnectionId='5' CharacterSpacing="0" x:Name="GridView" AutomationProperties.AutomationId="ItemGridView" ItemsSource="{Binding Photos}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}">
                        <GridView.ItemTemplate>
                            <DataTemplate                                        >
                                <Grid>
                                    <Border HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Image Source="{Binding Image}" Stretch="UniformToFill"/>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </GridView.ItemTemplate>
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapGrid x:ConnectionId='7' ItemWidth="{Binding DynamicPhotoSize}" 
                                      ItemHeight="{Binding DynamicPhotoSize}" 
                                      Orientation="Horizontal"
                                      MaximumRowsOrColumns="{Binding MaximumColumns}"
                                                                       />
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                        <GridView.ItemContainerStyle>
                            <Style TargetType="GridViewItem"> 
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0" />
                            </Style>
                        </GridView.ItemContainerStyle>
                    </GridView>

                    <Grid HorizontalAlignment="Stretch" Height="{Binding AvailableModalSize}" Width="{Binding AvailableModalSize}" Visibility="{Binding SelectedItem, Converter={StaticResource NullToVisibilityConverter}}">
                        <Border>
                            <Grid>
                                <Image x:ConnectionId='6' Stretch="UniformToFill" Source="{Binding SelectedItem.Image}" ManipulationMode="TranslateY"                                                                                            >
                                    <interactivity1:Interaction.Behaviors>
                                        <interactivity1:BehaviorCollection>
                                            <core:EventTriggerBehavior EventName="Tapped">
                                                <core:InvokeCommandAction Command="{Binding ShowLinkCommand}"></core:InvokeCommandAction>
                                            </core:EventTriggerBehavior>
                                        </interactivity1:BehaviorCollection>
                                    </interactivity1:Interaction.Behaviors>
                                </Image>
                                <Border Background="DarkSlateGray" Opacity="0.6" VerticalAlignment="Bottom" Visibility="{Binding IsShowLink, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" Padding="2">
                                        <TextBlock Opacity="1" Foreground="FloralWhite" Text="{Binding SelectedItem.FormattedAddress}" TextTrimming="CharacterEllipsis"></TextBlock>
                                        <HyperlinkButton Opacity="1" Foreground="FloralWhite" NavigateUri="{Binding SelectedItem.UserLink}" Content="{Binding SelectedItem.UserLink}" Margin="1"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </FlipViewItem>
        </FlipView>
        <Grid x:ConnectionId='2' x:Name="BottomToolBar" VerticalAlignment="Bottom" Background="DarkGray" Opacity="0.7">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <AppBarToggleButton x:ConnectionId='3' x:Name="MapToggleButton" Label="Карта" IsChecked="True">
                    <AppBarToggleButton.Icon>
                        <FontIcon FontFamily="{StaticResource FontAwesome}" Glyph="&#xf278;"></FontIcon>
                    </AppBarToggleButton.Icon>
                </AppBarToggleButton>
                <AppBarToggleButton x:ConnectionId='4' x:Name="GalleryToggleButton" Label="Галерея">
                    <AppBarToggleButton.Icon>
                        <FontIcon FontFamily="{StaticResource FontAwesome}" Glyph="&#xf03e;"></FontIcon>
                    </AppBarToggleButton.Icon>
                </AppBarToggleButton>
            </StackPanel>
        </Grid>
    </Grid>

</Page>

